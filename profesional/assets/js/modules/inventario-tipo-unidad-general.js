/* Tráfico App · Inventario · clasificación fija + catálogo · estable v11 */
(function(){
'use strict';
if(window.__CC_TIPO_UNIDAD_ESTABLE_V11__)return;
window.__CC_TIPO_UNIDAD_ESTABLE_V11__=true;
const sb=()=>window.gmSupabase;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let tipos=[],mapa=new Map(),loading=false,wrapped=false;
async function rpc(name,args={}){const c=sb();if(!c)throw new Error('Supabase no está disponible.');const r=await c.rpc(name,args);if(r.error)throw r.error;if(r.data?.ok===false)throw new Error(r.data.error||'Operación no disponible');return r.data;}
function tipoById(id){return tipos.find(x=>String(x.id)===String(id));}
function options(current=''){return '<option value="">Selecciona tipo de unidad</option>'+tipos.filter(x=>x.estatus==='ACTIVO'||String(x.id)===String(current)).map(x=>'<option value="'+esc(x.id)+'" '+(String(x.id)===String(current)?'selected':'')+'>'+esc(x.nombre)+'</option>').join('');}
function setSystemClass(base,tipo){if(!base||!tipo)return;const cat=String(tipo.categoria||'').toUpperCase()==='CARRO'?'CARRO':'CAJA';const opts=[...base.options];let opt=opts.find(o=>String(o.textContent||'').trim().toUpperCase()===cat);if(!opt&&cat==='CAJA')opt=opts.find(o=>String(o.textContent||'').trim().toUpperCase().includes('CAJA'));if(opt)base.value=opt.value;}
function cleanupLegacy(){
 document.querySelectorAll('[data-config-tipos-unidad-general]').forEach(x=>x.style.display='none');
 const oldSec=document.getElementById('ccConfigTiposUnidadGeneral');if(oldSec)oldSec.style.display='none';
 document.querySelectorAll('.cc-car-type-detail,[data-cc-tipo-unidad-general]').forEach(x=>x.remove());
 const extra=document.getElementById('ccInvTipoExtra');if(extra){extra.innerHTML='';extra.style.display='none';}
 ['ccInvPrintQr_20260913','ccInvPrintQrMoved','ccInventoryPrintQrDirectV61'].forEach(id=>document.getElementById(id)?.remove());
}
async function load(render=true){if(loading||!sb()||!window.CC_AUTH_READY)return;loading=true;try{const[a,b]=await Promise.all([rpc('cc_unit_type_catalog_list'),rpc('cc_unit_type_map')]);tipos=a?.rows||[];mapa=new Map((b?.rows||[]).map(x=>[String(x.numero||'').trim().toUpperCase(),x]));cleanupLegacy();installCatalog();if(render){decorateModals();decorateInventory();}}catch(e){console.warn('Tipos de unidad:',e)}finally{loading=false;}}
function decorateModal(modal){if(!modal)return;const f=modal.querySelector('form'),base=f?.querySelector('[name="tipoUnidadId"]'),num=f?.querySelector('[name="numero"]');if(!f||!base||!num)return;const baseField=base.closest('.cc-field');if(baseField){baseField.style.display='none';base.required=false;}let field=f.querySelector('[data-cc-tipo-unidad-catalogo]');if(!field){field=document.createElement('div');field.className='cc-field';field.dataset.ccTipoUnidadCatalogo='1';field.innerHTML='<label>Tipo de unidad</label><select name="tipoUnidadGeneralId" required></select><div style="font-size:9px;color:#64748b;margin-top:4px">Catálogo general. CAJA/CARRO se asigna automáticamente.</div>';baseField?.insertAdjacentElement('afterend',field);}const det=field.querySelector('select'),key=String(num.value||'').trim().toUpperCase(),mapped=mapa.get(key);if(det.dataset.ccUnitKey!==key){det.dataset.ccUnitKey=key;det.dataset.ccInitialized='';}if(!det.dataset.ccInitialized){let current=String(mapped?.tipoUnidadGeneralId||'');if(!current){const sys=String(base.selectedOptions?.[0]?.textContent||'').trim().toUpperCase()==='CARRO'?'CARRO':'CAJA';current=String(tipos.find(x=>x.estatus==='ACTIVO'&&String(x.categoria||'').toUpperCase()===sys)?.id||'');}det.innerHTML=options(current);if(current)det.value=current;det.dataset.ccInitialized='1';}if(!det.dataset.ccBound){det.dataset.ccBound='1';det.addEventListener('change',()=>setSystemClass(base,tipoById(det.value)));}setSystemClass(base,tipoById(det.value));}
function decorateModals(){decorateModal(document.getElementById('ccFormModal'));decorateModal(document.getElementById('ccEditUnitModal'));}
function decorateInventory(){const body=document.getElementById('ccInventarioBody');if(!body)return;[...body.querySelectorAll('tr')].forEach(tr=>{const td=tr.children;if(td.length<3||!td[1])return;const x=mapa.get(String(td[2]?.textContent||'').trim().toUpperCase());if(!x)return;const nombre=String(x.tipoUnidadGeneralNombre||'SIN TIPO');if(td[1].dataset.ccTipoCatalogo!==nombre){td[1].innerHTML='<span class="cc-unit-type-badge">'+esc(nombre)+'</span>';td[1].dataset.ccTipoCatalogo=nombre;}});}
async function persist(number,typeId){await rpc('cc_set_unit_type',{p_numero:number,p_tipo_id:typeId});await load(false);window.ccRenderInventario?.();window.ccAntLoad?.(true);}
document.addEventListener('submit',e=>{const f=e.target;if(!(f instanceof HTMLFormElement))return;const modal=f.closest('#ccFormModal,#ccEditUnitModal');if(!modal)return;const base=f.querySelector('[name="tipoUnidadId"]'),num=f.querySelector('[name="numero"]'),det=f.querySelector('[name="tipoUnidadGeneralId"]');if(!base||!num||!det)return;const typeId=String(det.value||''),tipo=tipoById(typeId);if(!typeId||!tipo){e.preventDefault();e.stopImmediatePropagation();alert('Selecciona el tipo de unidad.');return;}setSystemClass(base,tipo);const number=String(num.value||'').trim();let tries=0;const wait=()=>{if(!document.body.contains(modal)){persist(number,typeId).catch(err=>alert('La unidad se guardó, pero no se pudo sincronizar su tipo de unidad.\n\n'+err.message));return;}if(++tries<30)setTimeout(wait,250);};setTimeout(wait,300);},true);
function canEdit(){return window.CC_ACCESS?.rol==='ADMIN'||(typeof window.ccPerm==='function'&&window.ccPerm('configuracion.editar'));}
function renderCatalog(){const el=document.getElementById('ccTiposUnidadList');if(!el)return;el.innerHTML=tipos.length?tipos.map((x,i)=>'<tr><td>'+(i+1)+'</td><td><strong>'+esc(x.nombre)+'</strong></td><td><span class="cc-badge">'+esc(x.categoria||'')+'</span></td><td><span class="cc-badge '+(x.estatus==='ACTIVO'?'cc-ok':'cc-off')+'">'+esc(x.estatus||'ACTIVO')+'</span></td><td><button class="cc-btn cc-btn-light" data-unit-type-edit="'+esc(x.id)+'" '+(canEdit()?'':'disabled')+'>Editar</button></td></tr>').join(''):'<tr><td colspan="5" style="padding:30px;text-align:center;color:#94a3b8">No hay tipos configurados.</td></tr>';el.querySelectorAll('[data-unit-type-edit]').forEach(b=>b.onclick=()=>openCatalogForm(tipoById(b.dataset.unitTypeEdit)||{}));}
function openCatalogForm(x={}){if(!canEdit())return alert('Sin permiso para modificar Configuración.');document.getElementById('ccCanonicalTypeModal')?.remove();const ov=document.createElement('div');ov.id='ccCanonicalTypeModal';ov.style='position:fixed;inset:0;background:rgba(15,23,42,.76);z-index:100500;display:flex;align-items:center;justify-content:center;padding:16px';ov.innerHTML='<div style="background:#fff;width:min(560px,96vw);border-radius:16px;overflow:hidden"><div style="background:#0f172a;color:#fff;padding:15px 18px;display:flex;justify-content:space-between"><strong>'+(x.id?'Editar':'Nuevo')+' tipo de unidad</strong><button type="button" data-x style="border:0;background:none;color:#fff;font-size:22px">×</button></div><form style="padding:18px"><div class="cc-field"><label>Nombre del tipo de unidad</label><input name="nombre" required value="'+esc(x.nombre||'')+'"></div><div class="cc-field" style="margin-top:10px"><label>Clasificación del sistema</label><select name="categoria"><option value="CAJA" '+(x.categoria==='CAJA'?'selected':'')+'>CAJA</option><option value="CARRO" '+(x.categoria==='CARRO'?'selected':'')+'>CARRO</option></select></div><div class="cc-field" style="margin-top:10px"><label>Estatus</label><select name="estatus"><option '+(x.estatus!=='INACTIVO'?'selected':'')+'>ACTIVO</option><option '+(x.estatus==='INACTIVO'?'selected':'')+'>INACTIVO</option></select></div><div style="display:flex;justify-content:flex-end;gap:8px;margin-top:15px"><button type="button" class="cc-btn cc-btn-light" data-cancel>Cancelar</button><button class="cc-btn cc-btn-primary" type="submit">Guardar</button></div></form></div>';document.body.appendChild(ov);const close=()=>ov.remove();ov.querySelector('[data-x]').onclick=close;ov.querySelector('[data-cancel]').onclick=close;ov.querySelector('form').onsubmit=async e=>{e.preventDefault();const b=e.currentTarget.querySelector('[type=submit]');b.disabled=true;try{const fd=new FormData(e.currentTarget);await rpc('cc_unit_type_catalog_save',{p_item:{id:x.id||'',nombre:String(fd.get('nombre')||'').trim().toUpperCase(),categoria:String(fd.get('categoria')||'CARRO'),estatus:String(fd.get('estatus')||'ACTIVO')}});close();await load(false);renderCatalog();}catch(err){alert(err.message||err);b.disabled=false;}};}
function installCatalog(){const btn=document.querySelector('#ccPanelConfiguracion .cc-config-nav-btn[data-config="tiposUnidad"]');if(btn){btn.querySelector('span')&&(btn.querySelector('span').textContent='Tipos de unidad');btn.querySelector('small')&&(btn.querySelector('small').textContent='Catálogo general · CAJA/CARRO');}window.ccRenderTiposUnidad=renderCatalog;window.ccNuevoTipoUnidad=id=>openCatalogForm(tipoById(id)||{});window.ccDeleteTipoUnidad=()=>alert('Los tipos de unidad no se eliminan; puedes marcarlos INACTIVOS.');const add=document.querySelector('#ccConfigTiposUnidad .cc-config-section-head button');if(add)add.onclick=()=>openCatalogForm({categoria:'CARRO'});}
function wrapFunctions(){if(wrapped)return;wrapped=true;const r=window.ccRenderInventario;if(typeof r==='function'){window.ccRenderInventario=function(){const out=r.apply(this,arguments);cleanupLegacy();decorateInventory();return out;};}const e=window.ccEditarUnidadDirecto;if(typeof e==='function'){window.ccEditarUnidadDirecto=function(){const out=e.apply(this,arguments);setTimeout(decorateModals,0);return out;};}const n=window.ccNuevaCaja;if(typeof n==='function'){window.ccNuevaCaja=function(){const out=n.apply(this,arguments);setTimeout(decorateModals,0);return out;};}}
function boot(){const wait=setInterval(()=>{if(window.CC_AUTH_READY&&window.gmSupabase){clearInterval(wait);cleanupLegacy();wrapFunctions();installCatalog();load(true);}},200);setTimeout(()=>clearInterval(wait),15000);}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
