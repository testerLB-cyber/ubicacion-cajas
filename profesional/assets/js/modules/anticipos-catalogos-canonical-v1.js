/* Tráfico App · Anticipos · Catálogos canónicos v68 */
(function(){
'use strict';
if(window.__CC_ANT_CATALOGOS_CANONICAL_V68__)return;window.__CC_ANT_CATALOGOS_CANONICAL_V68__=true;
const sb=()=>window.gmSupabase,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),active=a=>(a||[]).filter(x=>String(x.estatus||'ACTIVO').toUpperCase()==='ACTIVO');
async function rpc(n,a={}){const c=sb();if(!c)throw Error('Supabase no está disponible.');const r=await c.rpc(n,a);if(r.error)throw r.error;if(r.data?.ok===false)throw Error(r.data.error||'Operación no disponible');return r.data;}
const canEdit=()=>window.CC_ACCESS?.rol==='ADMIN'||(typeof window.ccPerm==='function'&&window.ccPerm('anticipos.catalogos'));let data=null,selected='tarifas';
async function load(){const [base,dest]=await Promise.all([rpc('cc_ant_list'),rpc('cc_ant_destination_catalog')]);return data={base,dest};}
function modal(id,title,sub,html){document.getElementById(id)?.remove();const o=document.createElement('div');o.id=id;o.style='position:fixed;inset:0;background:rgba(15,23,42,.78);z-index:2147483500;display:flex;align-items:center;justify-content:center;padding:14px';o.innerHTML='<div style="background:#fff;width:min(900px,98vw);max-height:94vh;overflow:auto;border-radius:16px"><div style="background:#0f172a;color:#fff;padding:15px 18px;display:flex;justify-content:space-between"><div><strong>'+esc(title)+'</strong><div style="font-size:10px;color:#cbd5e1">'+esc(sub||'')+'</div></div><button type="button" data-x style="border:0;background:none;color:#fff;font-size:24px">×</button></div><div style="padding:18px">'+html+'</div></div>';document.body.appendChild(o);o.querySelector('[data-x]').onclick=()=>o.remove();return o;}
function opts(xs,label,current=''){return '<option value="">Seleccionar…</option>'+xs.map(x=>'<option value="'+esc(x.id)+'" '+(String(x.id)===String(current)?'selected':'')+'>'+esc(label(x))+'</option>').join('');}
function simpleList(xs,detail){return '<div style="border:1px solid #e2e8f0;border-radius:10px;overflow:hidden">'+(xs.length?xs.map(x=>'<div style="padding:10px 12px;border-bottom:1px solid #e2e8f0"><strong>'+esc(x.nombre||x.codigo||'')+'</strong><div style="font-size:10px;color:#64748b">'+esc(detail(x))+'</div></div>').join(''):'<div style="padding:20px;color:#64748b">Sin registros.</div>')+'</div>';}
async function openTariff(destinoId,typeId=''){
 if(!canEdit())return alert('No tienes permiso anticipos.catalogos.');const D=data||await load(),d=(D.base.destinos||[]).find(x=>String(x.id)===String(destinoId));if(!d)return alert('Destino no encontrado.');
 const types=active(D.dest.tiposUnidadGeneral),concepts=active(D.base.conceptos).filter(x=>x.uso_operadores!==false);let rows=[];
 const o=modal('ccAntTariffCanonical','Configuración por destino · '+d.nombre,'Selecciona el tipo de unidad y agrega conceptos uno por uno.','<div><div class="cc-field"><label>Tipo de unidad *</label><select data-type>'+opts(types,x=>x.nombre,typeId)+'</select></div><div style="margin-top:14px"><button type="button" class="cc-btn cc-btn-primary" data-add>+ Agregar concepto</button></div><div data-rows style="display:grid;gap:8px;margin-top:12px"></div><div style="display:flex;justify-content:flex-end;gap:8px;margin-top:16px"><button type="button" class="cc-btn cc-btn-light" data-cancel>Cancelar</button><button type="button" class="cc-btn cc-btn-primary" data-save>Guardar configuración</button></div></div>');
 const type=o.querySelector('[data-type]'),box=o.querySelector('[data-rows]'),save=o.querySelector('[data-save]');o.querySelector('[data-cancel]').onclick=()=>o.remove();
 function render(){box.innerHTML=rows.length?rows.map((r,i)=>'<div style="display:grid;grid-template-columns:minmax(0,1fr) 110px auto;gap:8px;align-items:center;padding:10px;border:1px solid #e2e8f0;border-radius:10px"><strong>'+esc(r.nombre)+'</strong><span>$'+Number(r.monto).toFixed(2)+'</span><button type="button" class="cc-btn cc-btn-danger" data-rm="'+i+'">Quitar</button></div>').join(''):'<div class="cc-note">Sin conceptos agregados.</div>';}
 function hydrate(){rows=[];const tid=type.value;if(tid)rows=(D.dest.destinoConceptosGeneral||[]).filter(x=>String(x.destinoId)===String(d.id)&&String(x.tipoUnidadGeneralId)===String(tid)&&String(x.estatus).toUpperCase()==='ACTIVO').map(x=>({conceptoId:String(x.conceptoId),nombre:x.concepto||'Concepto',monto:Number(x.monto||0)}));render();}
 type.onchange=hydrate;
 o.querySelector('[data-add]').onclick=()=>{if(!type.value)return alert('Primero selecciona el tipo de unidad.');const avail=concepts.filter(c=>!rows.some(r=>String(r.conceptoId)===String(c.id)));if(!avail.length)return alert('Ya agregaste todos los conceptos disponibles.');const m=modal('ccAntConceptPicker','Agregar concepto',d.nombre,'<div class="cc-field"><label>Concepto *</label><select data-concept>'+opts(avail,x=>x.nombre)+'</select></div><div class="cc-field" style="margin-top:10px"><label>Monto *</label><input data-amount type="number" inputmode="decimal" min="0.01" step="0.01"></div><div style="display:flex;justify-content:flex-end;gap:8px;margin-top:14px"><button type="button" class="cc-btn cc-btn-light" data-no>Cancelar</button><button type="button" class="cc-btn cc-btn-primary" data-ok>Agregar</button></div>');m.querySelector('[data-no]').onclick=()=>m.remove();m.querySelector('[data-ok]').onclick=()=>{const cid=String(m.querySelector('[data-concept]').value||''),amt=Number(m.querySelector('[data-amount]').value||0),c=concepts.find(x=>String(x.id)===cid);if(!cid)return alert('Selecciona un concepto.');if(!(amt>0))return alert('Captura un monto mayor a cero.');rows.push({conceptoId:cid,nombre:c?.nombre||'Concepto',monto:amt});m.remove();render();};};
 box.onclick=e=>{const b=e.target.closest('[data-rm]');if(!b)return;rows.splice(Number(b.dataset.rm),1);render();};
 save.onclick=async()=>{const tid=String(type.value||'');if(!tid)return alert('Selecciona tipo de unidad.');if(!rows.length)return alert('Agrega al menos un concepto.');save.disabled=true;save.textContent='Guardando…';try{const items=rows.map(r=>({conceptoId:r.conceptoId,monto:Number(r.monto),esDefault:true,estatus:'ACTIVO'}));await rpc('cc_ant_save_destination_config_general',{p_destino_id:d.id,p_tipo_unidad_general_id:tid,p_items:items});o.remove();data=null;await load();await renderCatalogs(false);alert('Configuración guardada correctamente.');}catch(err){alert('No se pudo guardar: '+(err.message||err));if(document.body.contains(save)){save.disabled=false;save.textContent='Guardar configuración';}}};
 if(typeId)type.value=typeId;hydrate();
}
function tariffList(D){const rr=(D.dest.destinoConceptosGeneral||[]).filter(x=>String(x.estatus).toUpperCase()==='ACTIVO'),g={};rr.forEach(x=>{const k=x.destinoId+'|'+x.tipoUnidadGeneralId;(g[k]??={destinoId:x.destinoId,tipoId:x.tipoUnidadGeneralId,tipo:x.tipoUnidadGeneral,items:[]}).items.push(x);});return '<div style="display:grid;gap:10px">'+active(D.base.destinos).map(d=>'<div style="border:1px solid #e2e8f0;border-radius:10px;padding:12px"><div style="display:flex;justify-content:space-between;gap:8px;align-items:center"><strong>'+esc(d.nombre)+'</strong><button class="cc-btn cc-btn-primary" data-config="'+esc(d.id)+'">Nueva configuración</button></div>'+Object.values(g).filter(x=>String(x.destinoId)===String(d.id)).map(x=>'<div style="margin-top:10px;padding-top:8px;border-top:1px solid #e2e8f0"><div style="display:flex;justify-content:space-between"><strong>'+esc(x.tipo)+'</strong><button class="cc-btn cc-btn-light" data-config="'+esc(d.id)+'" data-type="'+esc(x.tipoId)+'">Editar</button></div>'+x.items.map(i=>'<div class="cc-note">'+esc(i.concepto)+' · $'+Number(i.monto||0).toFixed(2)+'</div>').join('')+'</div>').join('')+'</div>').join('')+'</div>';}
async function renderCatalogs(force=false){const root=document.getElementById('ccAntViewCatalogos');if(!root)return;if(force||!data)await load();const D=data;root.innerHTML='<div class="cc-config-alert"><strong>Catálogos de Anticipos</strong> · La edición requiere permiso <code>anticipos.catalogos</code>.</div><div id="ccAntCanonicalSelector" style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin:16px 0"><button class="cc-btn cc-btn-light" data-cat="cuentas">Cuentas</button><button class="cc-btn cc-btn-light" data-cat="conceptos">Conceptos</button><button class="cc-btn cc-btn-light" data-cat="destinos">Destinos</button><button class="cc-btn cc-btn-light" data-cat="metodos">Métodos de depósito</button><button class="cc-btn cc-btn-light" data-cat="movimientos">Tipos de movimiento de caja</button><button class="cc-btn cc-btn-light" data-cat="tarifas">Configuración por destino</button></div><div data-content style="padding-top:8px"></div>';const content=root.querySelector('[data-content]');function show(k){selected=k;root.querySelectorAll('[data-cat]').forEach(b=>b.className='cc-btn '+(b.dataset.cat===k?'cc-btn-primary':'cc-btn-light'));if(k==='cuentas')content.innerHTML=simpleList(active(D.base.cuentas),x=>(x.tipo||'')+' · Saldo inicial '+Number(x.saldo_inicial||0).toFixed(2));if(k==='conceptos')content.innerHTML=simpleList(active(D.base.conceptos),x=>x.categoria||x.estatus);if(k==='destinos')content.innerHTML=simpleList(active(D.base.destinos),x=>x.descripcion||x.estatus);if(k==='metodos')content.innerHTML=simpleList(active(D.base.metodosDeposito),x=>x.descripcion||x.estatus);if(k==='movimientos')content.innerHTML='<div class="cc-note">Este catálogo conserva su administración actual.</div>';if(k==='tarifas')content.innerHTML=tariffList(D);}root.querySelector('#ccAntCanonicalSelector').onclick=e=>{const b=e.target.closest('[data-cat]');if(b)show(b.dataset.cat);};content.onclick=e=>{const b=e.target.closest('[data-config]');if(b)openTariff(b.dataset.config,b.dataset.type||'');};show(selected);}
function install(){const old=window.ccAntView;if(typeof old==='function'&&!old.__canonicalCats68){const w=function(v){const r=old.apply(this,arguments);if(String(v)==='catalogos')setTimeout(()=>renderCatalogs(true).catch(e=>alert(e.message||e)),0);return r;};w.__canonicalCats68=true;window.ccAntView=w;}if(document.getElementById('ccAntViewCatalogos'))renderCatalogs(true).catch(()=>{});}const t=setInterval(()=>{if(window.gmSupabase&&typeof window.ccAntView==='function'){clearInterval(t);install();}},200);setTimeout(()=>{clearInterval(t);install();},5000);
})();