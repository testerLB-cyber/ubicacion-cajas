name: Aplicar Control Hojas v10.2

on:
  push:
    branches: [ main ]
    paths:
      - 'profesional/assets/js/modules/service-sheets-control-v10-2.js'
      - '.github/workflows/apply-service-sheets-v10-2.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Activar módulo v10.2
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('profesional/index.html')
          s=p.read_text(encoding='utf-8')
          tag='<script src="assets/js/modules/service-sheets-control-v10-2.js?v=v10-2"></script>'
          if tag not in s:
              anchors=[
                '<script src="assets/js/modules/service-sheets-control-v10-1.js?v=v10-1"></script>',
                '<script src="assets/js/modules/service-sheets-control-v10-1.js"></script>',
                '<script src="assets/js/modules/service-sheets-comprobacion-v9.js?v=v9"></script>'
              ]
              for a in anchors:
                  if a in s:
                      s=s.replace(a,a+'\n'+tag,1)
                      break
              else:
                  raise SystemExit('No se encontró ancla de Control de Hojas')
              p.write_text(s,encoding='utf-8')
          PY
      - name: Validar JavaScript
        run: node --check profesional/assets/js/modules/service-sheets-control-v10-2.js
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profesional/index.html
          if ! git diff --cached --quiet; then
            git commit -m "Activar Control de Hojas v10.2"
            git push
          fi
